/**
 * Глобальные флаги контента. Тарифы пакетов пока не финализированы — стоимость скрыта в UI и в разметке.
 */
export function useSiteFlags()
{
	const showParticipationPricing = false;

	return {
		showParticipationPricing,
	};
}
